//first import the os module i.e operating system moudule from Node js OS documentation
const os = require('os');

//shows the free memory space of my computer
console.log(os.freemem())
//shows the home directory
console.log(os.homedir())
//shows the hostname
console.log(os.hostname())
//shows the platform
console.log(os.platform())
//shows the release
console.log(os.release())
//shows the release
console.log(os.type())